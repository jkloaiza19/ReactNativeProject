import React from 'react'
import { View } from 'react-native'

export const NavigationTabBar = ():React.ReactNode => (
  <View style={{ flex: 1, width: '100%', height: '100%', backgroundColor: '#000' }} />
)